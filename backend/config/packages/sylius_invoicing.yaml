imports:
    - { resource: "@SyliusInvoicingPlugin/config/config.yaml" }

# PDF actif via sylius/pdf-generation-bundle et son adaptateur knp_snappy.
# L'installation explicite de wkhtmltopdf est documentee dans docs/invoices.md.
sylius_invoicing:
    pdf_generator:
        enabled: true
        legacy: false
